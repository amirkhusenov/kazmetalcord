// We are type asserting, because, we validate them in parsedEnv.ts in server side
export const parsedClientEnv : Record<string, string> = {
  // NEXT_PUBLIC_CDN_BASE_URL: process.env.NEXT_PUBLIC_CDN_BASE_URL!,
};
