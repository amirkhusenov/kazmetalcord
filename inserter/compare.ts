// import sorted from "./categories.json";
// import unsorted from "./category-tree.json";
// import stringify from "fast-json-stable-stringify";

// console.log(`${sorted.length} sorted categories`);
// console.log(`${unsorted.length} unsorted categories`);

// const sortedStr = stringify(sorted).normalize("NFC");
// const unsortedStr = stringify(unsorted).normalize("NFC");

// console.log(`Are they equal? ${sortedStr === unsortedStr}`);

